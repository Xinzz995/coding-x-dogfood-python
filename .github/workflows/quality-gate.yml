name: Quality Gate

# Generated from .coding-x/quality.json. Change the contract, then regenerate.
on:
  pull_request:
  push:
    branches: ['main']

permissions:
  contents: read

concurrency:
  group: quality-gate-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  checks_linux-primary:
    name: checks / linux-primary
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
      - name: 'toolchain / python 3.12'
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
      - name: 'setup / 1'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pip'' ''install'' ''-e'' ''.[dev]'''
      - name: 'setup / 2'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pip'' ''install'' ''-e'' ''.[dev]'''
      - name: 'test / test-packages-api'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pytest'''
      - name: 'test / test-packages-worker'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pytest'''
      - name: 'build / build-packages-api'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pip'' ''wheel'' ''.'' ''--no-deps'' ''--wheel-dir'' ''dist'''
      - name: 'build / build-packages-worker'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pip'' ''wheel'' ''.'' ''--no-deps'' ''--wheel-dir'' ''dist'''
      - name: 'static / ruff-packages-api'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''ruff'' ''check'' ''.'''
      - name: 'static / ruff-packages-worker'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''ruff'' ''check'' ''.'''
  checks_macos-primary:
    name: checks / macos-primary
    runs-on: macos-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
      - name: 'toolchain / python 3.12'
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
      - name: 'setup / 1'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pip'' ''install'' ''-e'' ''.[dev]'''
      - name: 'setup / 2'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pip'' ''install'' ''-e'' ''.[dev]'''
      - name: 'test / test-packages-api'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pytest'''
      - name: 'test / test-packages-worker'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pytest'''
      - name: 'build / build-packages-api'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pip'' ''wheel'' ''.'' ''--no-deps'' ''--wheel-dir'' ''dist'''
      - name: 'build / build-packages-worker'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''pip'' ''wheel'' ''.'' ''--no-deps'' ''--wheel-dir'' ''dist'''
      - name: 'static / ruff-packages-api'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''ruff'' ''check'' ''.'''
      - name: 'static / ruff-packages-worker'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: bash
        run: '''python'' ''-m'' ''ruff'' ''check'' ''.'''
  checks_windows-primary:
    name: checks / windows-primary
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
      - name: 'toolchain / python 3.12'
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12'
      - name: 'setup / 1'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: pwsh
        run: '& ''python'' ''-m'' ''pip'' ''install'' ''-e'' ''.[dev]'''
      - name: 'setup / 2'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: pwsh
        run: '& ''python'' ''-m'' ''pip'' ''install'' ''-e'' ''.[dev]'''
      - name: 'test / test-packages-api'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: pwsh
        run: '& ''python'' ''-m'' ''pytest'''
      - name: 'test / test-packages-worker'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: pwsh
        run: '& ''python'' ''-m'' ''pytest'''
      - name: 'build / build-packages-api'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: pwsh
        run: '& ''python'' ''-m'' ''pip'' ''wheel'' ''.'' ''--no-deps'' ''--wheel-dir'' ''dist'''
      - name: 'build / build-packages-worker'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: pwsh
        run: '& ''python'' ''-m'' ''pip'' ''wheel'' ''.'' ''--no-deps'' ''--wheel-dir'' ''dist'''
      - name: 'static / ruff-packages-api'
        working-directory: 'packages/api'
        timeout-minutes: 10
        shell: pwsh
        run: '& ''python'' ''-m'' ''ruff'' ''check'' ''.'''
      - name: 'static / ruff-packages-worker'
        working-directory: 'packages/worker'
        timeout-minutes: 10
        shell: pwsh
        run: '& ''python'' ''-m'' ''ruff'' ''check'' ''.'''
  quality-gate:
    name: quality-gate
    if: ${{ always() }}
    needs: [checks_linux-primary, checks_macos-primary, checks_windows-primary]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions: {}
    steps:
      - name: Verify every required job completed successfully
        shell: bash
        env:
          RESULT_1: ${{ needs.checks_linux-primary.result }}
          RESULT_2: ${{ needs.checks_macos-primary.result }}
          RESULT_3: ${{ needs.checks_windows-primary.result }}
        run: |
          failed=0
          for result in "${!RESULT_@}"; do
            value="${!result}"
            if [ "$value" != "success" ]; then
              echo "::error::${result}=${value}; required jobs must not fail, cancel, time out, or skip"
              failed=1
            fi
          done
          exit "$failed"
